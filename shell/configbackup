#!/bin/bash
exename=$(basename $0)
backup=~/configbackup
echo $exename
case $1 in
    copy)
sudo cp -r ~/shell/ $backup
sudo cp -r ~/.vim $backup
sudo cp ~/.vimrc $backup
sudo cp ~/.tmux.conf $backup
        ;;
    clone)
	git clone https://github.com/adambynes/configbackup.git
        ;;
 
    *)
        ;;
esac
