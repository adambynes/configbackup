#!/bin/bash
exename=$(basename $0)
backup=~/configbackup
echo $exename
case $exename in
    oigrep)
        ;;
 
    *)
        ;;
esac
sudo cp -r ~/shell/ $backup
sudo cp -r ~/.vim $backup
sudo cp ~/.vimrc $backup
sudo cp ~/.tmux.conf $backup
